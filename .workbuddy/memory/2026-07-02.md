## 2026-07-02

### Dashboard 布局调整
- **API 总数移至左侧**：将 `api-count-panel` 改为 flex 横向布局，左侧为 `.api-count-total`（大数字 + "API 总数" 标签），右侧为四个类型数量卡片
- **类型卡片减小**：`padding` 从 14px 16px 减为 10px 14px，`min-height` 从 80px 减为 64px，`.value` 字号从 1.6rem 减为 1.35rem，`.label` 从 0.85rem 减为 0.78rem，gap 从 16px 减为 10px
- 装饰圆从 64px → 48px，透明度从 0.15 → 0.12

